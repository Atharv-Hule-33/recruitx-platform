spring:
  application:
    name: user-service

  config:
    uri: http://localhost:8888
    fail-fast: true
    retry:
      max-attempts: 5
      initial-interval: 2000
      multiplier: 1.5
      max-interval: 10000

config:
  import: "configserver:"
